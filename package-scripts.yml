scripts:
  test:
    run: vitest run
    temp: vitest run list.spec
    ui: vitest --ui --outputFile tests/dist/vite/html
    preview: vitest preview
    html: vite preview --outDir tests/dist/vite/

  dev: nodemon
